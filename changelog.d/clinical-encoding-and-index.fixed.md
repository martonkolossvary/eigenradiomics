`encode_clinical_series` now treats a column as numeric only when most non-missing values parse as numbers, so a lone numeric token in an otherwise categorical column no longer blanks out the rest. `compute_clinical_correlations` now raises a distinct, descriptive error when the feature and clinical tables share no common index labels, instead of silently producing all-NaN correlations misreported as "not enough data".
