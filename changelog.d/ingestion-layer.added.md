Added a data-ingestion layer for the pre-analysis entry point: `FeatureCatalog` (first-class `config__feature_key` catalog with annotation/validation/grouping), study-agnostic ingestion primitives (`normalize_id_column`, `resolve_duplicates`, `merge_tables` with alignment diagnostics), and `RadiomicsDataset` / `StudyDesign` — a container that separates feature columns from metadata and exposes `groups` / `y` / `to_pipeline_input()` for leakage-safe scikit-learn pipelines.
